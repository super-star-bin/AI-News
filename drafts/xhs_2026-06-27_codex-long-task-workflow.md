# 小红书草稿：我用 Codex 跑了一次长任务工作流

topic_id: T20260627-002  
栏目：工具实测  
方向：AI 工具实测  
原始链接：https://arxiv.org/abs/2606.26959  
事实核验：`factchecks/factcheck_2026-06-27_codex-long-task-workflow.md`

## 标题备选

1. 我用 Codex 跑了一次长任务工作流
2. 长任务交给 AI，关键不是一口气写完
3. Codex 适合做长任务吗？我测了一次
4. 把 AI 当执行编辑，必须先有检查点
5. AI 长任务真正难在可恢复和可验收

## 封面文案备选

1. 长任务交给 AI，先设检查点
2. Codex 不怕任务长，怕边界不清
3. 我用 Codex 做了一天内容生产

## 8 页卡片文案

### 第 1 页：封面

长任务交给 AI  
先设检查点

副标题：Codex 适合跑流程，但不能替你承担责任。

### 第 2 页：先说结论

Codex 适合做多文件、长流程、可校验的任务。

但前提是你要给它目录结构、事实规则、输出标准和验证命令。否则长任务只会把小错误放大。

### 第 3 页：论文背景

arXiv 论文 `The Shift to Agentic AI: Evidence from Codex`，中文可理解为“从 Codex 证据看 AI 使用正在转向 Agent 化”。

摘要称，Codex 使用中并发 Agent、skills 和长任务占比都在上升。

### 第 4 页：我实际测了什么

这次测试不是自动发布小红书。

我让 Codex 完成 2026-06-27 的每日内容生产：选 4 个 S 级来源、写简报、更新 topics、生成事实核验、草稿、source.json、publish_text，并渲染卡片。

### 第 5 页：它做得好的地方

- 能按日期目录组织输出。
- 能把正文和卡片同步。
- 能保留来源和限制条件。
- 能跑渲染和数据校验。

### 第 6 页：它容易出问题的地方

长任务最怕三件事：来源没核验、路径没统一、正文和图文不同步。

所以不能只给一句“帮我做完”，必须给清楚的规范和验收标准。

### 第 7 页：适合哪些任务

适合交给它：每日简报、资料整理、竞品对比、内容草稿、发布检查清单。

不适合直接交给它：账号发布、高风险事实判断、法律财务医疗结论。

### 第 8 页：我的建议

把长任务拆成 4 层：

输入来源、生成草稿、机器校验、人工审核。

让 AI 跑流程，让人做判断。

## 正文

今天做一条工具实测：长任务到底适不适合交给 Codex？

结论：适合，但不是“甩一句话让它做完”。Codex 更适合处理多文件、长流程、可校验的任务。你要先给目录结构、内容标准、事实边界和验证命令，它才能把长任务做成可检查的工作流。

背景来源是 arXiv 论文：`The Shift to Agentic AI: Evidence from Codex`。中文可以理解为：从 Codex 证据看 AI 使用正在转向 Agent 化。论文摘要称，Codex 使用中并发 agents、skills 和超过 8 小时的长任务占比都在上升。

这些是论文观察，不等于我的实测结论。我的本地实测很具体：让 Codex 在这个小红书 AI 账号工作区里，完成 2026-06-27 的每日待发布内容生产。

任务不是“写一篇文章”，而是一整套流程：选 4 个 S 级来源，写原始链接和 brief，更新 `data/topics.csv`，生成事实核验、草稿、`source.json`、`publish_text.md`，再渲染卡片并跑数据校验。

这类任务最能体现 Codex 的优势：它不是只输出一段文字，而是在多个文件之间保持一致。草稿有正文，发布正文有栏目，`source.json` 有卡片，渲染后还能检查数量和路径。

我观察到它适合两类工作。

第一类是内容生产流程。单人运营账号每天要做热点、工具、开源、Agent 观察，最累的不是标题，而是整理来源、同步正文、补风险、改目录、跑校验。Codex 适合把这些步骤串起来。

第二类是产品或运营研究。比如做竞品分析，不要只写“帮我分析竞品”，而要给它官网页面、字段、输出格式和不确定点要求。这样 Codex 可以按结构跑完，人再判断。

但长任务也会放大错误。短任务错一句很容易发现；长任务如果一开始来源选错、路径写错、事实边界没说清，后面所有文件都会跟着错。

交给 Codex 前，先准备 4 个东西：目录结构；事实规则，说明哪些必须核验、哪些只能写“不确定”；检查点，先确认选题再写正文、渲染、验证；验收命令，比如 `npm run validate:data`。

限制也要说清楚：这不是对 Codex 所有能力的全面评测，也没有测试自动发布小红书、登录态页面或账号操作。它能提高流程效率，但不能替你承担内容责任。

一句话总结：Codex 适合跑长任务，但长任务真正需要的不是“更强的提示词”，而是可恢复的文件结构、清楚的事实边界和可执行的验收标准。

## 适合谁

- 单人运营内容账号的人。
- 需要把资料整理成固定格式的人。
- 产品、运营、研究同学，需要做竞品、复盘、需求分类的人。
- 已经有明确目录、模板和校验规则的团队。

## 不适合谁

- 想让 AI 自动发布、自动登录、自动承担账号风险的人。
- 没有事实核验流程，却想高频生产内容的人。
- 希望一句话让 AI 完成复杂任务且不检查的人。

## 需要人工补充

- [ ] 发布前确认本地渲染和数据校验已经完成。
- [ ] 如果配图，可放一张 `outputs/xhs/2026-06-27/` 目录截图，但不要露出敏感路径。
- [ ] 不把论文里的 Codex 使用趋势写成所有 AI Agent 的普遍结论。

## 评论区引导

- 你会把长任务交给 AI 吗？
- 你最担心 AI 长任务哪里出错：事实、格式，还是中途跑偏？
- 如果做一份“AI 长任务委托清单”，你想看吗？
